# Dashboard maintenance rules

- 사용자에게 보이는 대시보드 기능, 화면, 계산식 또는 데이터 처리 로직을 변경할 때마다 `CHANGELOG.md`의 최상단 날짜 섹션에 간결한 한국어 패치노트를 함께 추가한다.
- 단순 질의응답이나 파일을 변경하지 않은 작업은 패치노트에 기록하지 않는다.
- CSV 데이터 갱신 이력은 `update_data_log.py`와 업로드 스크립트가 관리하므로 수동으로 중복 추가하지 않는다.
